// Point this at your deployed backend (Render/Railway/Fly/VPS/etc).
// Leave it empty ("") only for local dev, where the backend serves this
// frontend itself and same-origin requests work automatically.
//
// Example once your backend is deployed:
//   window.SOC_API_BASE = "https://soc-dashboard-api.onrender.com";
window.SOC_API_BASE = "";
