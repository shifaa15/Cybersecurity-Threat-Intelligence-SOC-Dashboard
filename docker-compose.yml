services:
  soc-dashboard:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    volumes:
      - soc-data:/app/backend/data
    restart: unless-stopped

volumes:
  soc-data:
